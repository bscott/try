# Binaries
# Root-only — bare "try" would also match the cmd/try/ directory.
/try
*.exe
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html

# Build artifacts
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
